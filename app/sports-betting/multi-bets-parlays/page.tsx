import BlogPost from './BlogPost';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Multi Bets & Parlays: Maximize Your Winnings | IK118',
  description: 'Master multi bets and parlays with IK118. Learn strategies to build winning combinations and manage risk.',
  keywords: 'multi bets, parlays, IK118 betting strategy, accumulator bets, combo bets, parlay strategy',
};

export default function MultiBetsParlaysPage() {
  return <BlogPost />;
}
